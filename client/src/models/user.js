export default class User {
  constructor(name, lastname, userName, city, email, password) {
    this.name = name;
    this.lastname = lastname;
    this.userName = userName;
    this.city = city;
    this.email = email;
    this.password = password;
  }
}